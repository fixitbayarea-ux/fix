import React from 'react';

const FloatingButtons = () => {
  // This component is now disabled - Vapi.ai chat widget is the only floating CTA
  // Orange Book/Call buttons are removed per requirements
  return null;
};

export default FloatingButtons;